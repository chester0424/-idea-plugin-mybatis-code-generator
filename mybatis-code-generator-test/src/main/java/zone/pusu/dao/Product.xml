<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="zone.pusu.dao.IProductDao">
    <resultMap id="resultMap" type="zone.pusu.Product">
        <result property="id" column="ID" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="amount" column="amount" jdbcType="INTEGER"/>
    </resultMap>

    <select id="get">
        SELECT * FROM Product
        WHERE id = #{id}
    </select>

    <select id="getPageList" resultType="resultMap">
        SELECT * FROM Product
        <trim prefix="WHERE" prefixOverrides="and |or ">
            <if test="id != null">and t.ID like #{id}</if>
            <if test="name != null">and t.name like #{id}</if>
            <if test="amount != null">and t.amount like #{id}</if>
        </trim>
    </select>

    <insert id="inert">
        INSERT INTO Product(
            id,
            name,
            amount
        )
        VALUES (
            #{id,jdbcType=VARCHAR},
            #{name,jdbcType=VARCHAR},
            #{amount,jdbcType=INTEGER},
        )
    </insert>

    <update id="Update">
        UPDATE Product
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="amount != null">amount = #{amount},</if>
        </set>
        where id = #{id}
    </update>

    <delete id="delete">
        DELETE Product WHERE id = #{id}
    </delete>
</mapper>